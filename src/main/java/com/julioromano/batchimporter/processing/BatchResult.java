package com.julioromano.batchimporter.processing;

/**
 * {@code BatchResult} is a superclass to represent the result
 * of the process.
 *
 * Whenever a new Batch type is added to the system, a new subclass
 * of {@code BatchResult} should be created with its
 * required fields, representing the result of that specific process.
 *
 * @author  Julio Romano
 * @since   1.0
 */
public abstract class BatchResult {
}
